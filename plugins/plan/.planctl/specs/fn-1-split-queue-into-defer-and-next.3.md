## Description

**Size:** M
**Files:** skills/defer/SKILL.md, skills/queue/ (delete), skill-templates/queue-defer.md.tmpl (delete), skills/defer/SKILL.md.managed-file-dont-edit (delete), skills/queue/SKILL.md.managed-file-dont-edit (delete), .gitignore, tests/test_queue_defer_skill_consistency.py → tests/test_defer_skill_consistency.py

Remove `/plan:queue` and convert `defer` from a generated template output
into a hand-written tracked source skill. With only one variant left, the
shared template has no reason to exist.

### Approach

- Rewrite `skills/defer/SKILL.md` as a clean hand-written source skill:
  strip the `Generated by:` provenance header and EVERY queue conditional
  branch, leaving only the defer behavior; set `name: defer` (drop the
  `plan:` prefix). Keep `argument-hint`, `allowed-tools`, and the
  existing defer body/guardrails. It must NOT contain `queue_jump: true`
  anywhere (defer omits the field).
- Delete `skills/queue/` (whole dir), `skills/queue/SKILL.md.managed-file-dont-edit`,
  `skills/defer/SKILL.md.managed-file-dont-edit`, and
  `skill-templates/queue-defer.md.tmpl`.
- `.gitignore`: remove the `skills/queue/` and `skills/defer/` lines (defer
  is now tracked); KEEP `skills/work/` (still generated).
- Replace `tests/test_queue_defer_skill_consistency.py` with
  `tests/test_defer_skill_consistency.py`: single-skill assertions —
  `skills/defer/SKILL.md` exists, `name: defer`, contains no
  `queue_jump: true` literal, references `planctl scaffold`, and every
  `planctl <verb>` in a fenced bash block resolves via `--help` (the
  fn-339 guard). Drop the two-variant byte-identity machinery, the
  `Generated by:` assertion, and all queue-specific tests.

Do NOT touch `planctl/run_scaffold.py`, `planctl/models.py`, or
`tests/test_scaffold.py` — scaffold's optional `queue_jump` YAML field
stays wired as a valid raw-scaffold input.

### Investigation targets

**Required** (read before coding):
- skills/defer/SKILL.md — the current generated output to convert to clean source.
- skill-templates/queue-defer.md.tmpl — the template to delete (read it to know which conditionals to drop).
- tests/test_queue_defer_skill_consistency.py — the test being replaced.
- .gitignore (lines ~34-38) — the generated-skills block; remove queue/defer, keep work.

## Acceptance

- [ ] `skills/defer/SKILL.md` is tracked source: `name: defer`, no `Generated by:` header, no queue conditionals, no `queue_jump: true`.
- [ ] `skills/queue/`, `skill-templates/queue-defer.md.tmpl`, and both `.managed-file-dont-edit` sidecars are gone.
- [ ] `.gitignore` no longer ignores `skills/queue/` or `skills/defer/`; still ignores `skills/work/`.
- [ ] `tests/test_defer_skill_consistency.py` passes; the old `tests/test_queue_defer_skill_consistency.py` is gone.

## Done summary
Converted skills/defer/SKILL.md to hand-written tracked source (name: defer, no Generated-by header, no queue conditionals, no queue_jump literal); deleted skills/queue/, skill-templates/queue-defer.md.tmpl, and both .managed-file-dont-edit sidecars; untracked queue/defer in .gitignore (work stays ignored); replaced test_queue_defer_skill_consistency.py with test_defer_skill_consistency.py.
## Evidence
