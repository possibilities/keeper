{
  "name": "keeper-plan-hooks",
  "version": "0.0.0",
  "description": "Bun TypeScript hook dispatchers enforcing the content-blind orchestrator contract for the keeper plan plugin",
  "private": true,
  "scripts": {
    "build": "bun build --compile src/cli.ts --outfile dist/keeper-plan-bun && chmod +x dist/keeper-plan-bun",
    "promote": "bash scripts/promote.sh",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "biome check --no-errors-on-unmatched plugin/hooks test src",
    "test:gate": "bun ../../scripts/test-gate.ts --phase=plan --timeout 30000",
    "test": "bun run test:gate"
  },
  "dependencies": {
    "yaml": "2.8.1"
  },
  "devDependencies": {
    "@biomejs/biome": "2.2.0",
    "@types/bun": "^1"
  },
  "engines": {
    "bun": "1.3.13"
  }
}
