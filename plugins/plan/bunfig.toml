[test]
# Bare discovery runs the shared fail-fast sentinel. Keep the entrypoint first so
# its aggregate marker is consumed before matrix setup or test code executes.
root = "../../scripts/test-entrypoint.ts"
preload = ["../../scripts/test-entrypoint.ts", "./test/preload-host-matrix.ts"]
