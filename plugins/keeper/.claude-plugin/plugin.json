{
  "name": "keeper",
  "description": "Write one row per Claude Code hook invocation into keeper.db for the reducer daemon to fold, and hard-deny subagents from creating/switching git branches via a PreToolUse(Bash) branch-guard",
  "author": {"name": "arthack"}
}
