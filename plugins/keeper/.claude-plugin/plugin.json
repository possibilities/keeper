{
  "name": "keeper",
  "description": "Write one row per Claude Code hook invocation into keeper.db for the reducer daemon to fold, hard-deny subagents from creating/switching git branches via a PreToolUse(Bash) branch-guard, and auto-arm the Agent Bus inbox watcher (keeper bus watch) as an always-on session Monitor",
  "author": {"name": "arthack"},
  "experimental": {
    "monitors": "./monitors.json"
  }
}
