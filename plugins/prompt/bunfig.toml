[test]
# Bare discovery runs only the shared fail-fast sentinel. Named gates pass the
# package test directory explicitly; targeted `./*.test.ts` paths stay available.
root = "../../scripts/test-entrypoint.ts"
preload = ["../../scripts/test-entrypoint.ts"]
