## Overview

Fold six verified craft deltas from Matt Pocock's skillset into keeper's prompt layer: an intent-archaeology step in the merge-resolver brief, a tautological-test guard in the worker template, a single-source landed-vs-complete snippet replacing three near-verbatim copies, a progressive-disclosure split of the oversized plan skill, two new authoring levers (two-load frame, Leitwort-as-compression), and recommended answers on every planning priority question. All mechanical; the sibling domain-knowledge epic builds on these files afterward.

## Quick commands

- `bun test test/daemon.test.ts` — resolver brief assertions green after the graft
- `bun scripts/vendor-corpus.ts --check` — corpus + BAKE regions drift-free after the snippet work
- `cd plugins/plan && bun test` — plan suite green (generated-guard passes after worker re-render)
- `wc -l plugins/plan/skills/plan/SKILL.md` — visibly smaller after the split

## Acceptance

- [ ] Resolver brief instructs primary-source intent archaeology before classification and forbids inventing new behaviour, with abort-on-unclear semantics unchanged
- [ ] Worker template carries the independent-source-of-truth test rule; rendered worker cells match the template
- [ ] The landed-vs-complete kernel exists once in the vendored corpus; hack BAKEs it, plan and await POINTER it, and the drift gate passes
- [ ] plan/SKILL.md discloses operator-orchestration detail behind a pointer and is materially smaller; the one-task/ticket-vs-fog guidance has one authoritative statement per surface with pointers elsewhere
- [ ] skill-authoring.md teaches the two-load frame and Leitwort-as-compression as distinct levers
- [ ] Plan Phase 2d requires a recommended answer attached to every priority question

## Early proof point

Task that proves the approach: task 1 (resolver graft — smallest, purely additive prompt strings with existing test coverage). If it fails: the graft wording conflicts with test assertions; rework the assertions with it in one commit.

## References

- Study reports and settled direction: scratchpad matt-meta-report.md, matt-workflows-report.md, keeper-map-report.md (session artifacts)
- `fn-1098` (overlap+dep) — in-flight resolver-first flow edits the same daemon resolver region; this epic lands after it
- `fn-1099` (overlap) — its task edits plan/SKILL.md Phase 5e; sequenced via epic dep

## Docs gaps

- **plugins/plan/CLAUDE.md**: the baked-snippet enumeration ("bakes four shared keeper prompt snippets") goes stale when hack gains its fifth BAKE — task 5 updates it in the same commit
