## Overview

A dirty (non-mid-merge) shared checkout degrades every epic finalize into a silent non-sticky retry skip — one dirty file once stalled four epics with no loud surface. Extend the landed shared-checkout-wedge grace-watermark distress idiom to the plain-dirty case: a repo whose finalizes keep dirt-skipping past a short grace mints ONE self-clearing per-repo needs_human distress row, level-cleared when a recover cycle observes the checkout clean.

## Quick commands

- `bun test test/autopilot-worker.test.ts test/dispatch-failure-key.test.ts` — tracker lifecycle + key/pill suites

## Acceptance

- [ ] A persistently-dirty shared checkout surfaces as exactly one self-clearing per-repo distress row past the grace watermark, never cross-clearing with the mid-merge wedge row, with pre-grace skips still minting nothing and the mid-merge path unchanged
