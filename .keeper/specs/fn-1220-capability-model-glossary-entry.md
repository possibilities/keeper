## Overview

The glossary defines Worker cell, Provider, Pecking order, Wrapped cell, and Wrapper driver, but "capability model" — the model-axis value a task carries, which a provider roster resolves to a serving harness and native id — is used across the wrapped-cell code and worker contract without a definition. Add the entry in the established glossary register.

## Quick commands

- `grep -A2 "Capability model" CONTEXT.md` — the entry exists in the session-surface/worker-cell cluster with an Avoid line

## Acceptance

- [ ] CONTEXT.md defines Capability model in the established entry shape (one-sentence role-and-behavior definition plus an Avoid line), placed with the worker-cell vocabulary cluster, without duplicating or contradicting the existing Provider and Wrapped cell entries
