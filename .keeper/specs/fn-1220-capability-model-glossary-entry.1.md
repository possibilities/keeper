## Description

**Size:** S
**Files:** CONTEXT.md

### Approach

Add one glossary entry to CONTEXT.md: Capability model — the model-axis value a task carries (e.g. a bare capability token), which the host provider roster resolves at run time to a serving harness and native model id; native when the claude provider carries it, wrapped otherwise. Follow the established entry register exactly: bold term, one-sentence role-and-behavior definition, an Avoid line naming rejected synonyms (e.g. harness model, backend model, model alias). Place it adjacent to the existing Provider / Wrapped cell cluster. Touch nothing else.

## Acceptance

- [ ] CONTEXT.md carries a Capability model entry in the established shape (definition + Avoid line) within the worker-cell vocabulary cluster, consistent with the neighboring Provider and Wrapped cell entries

## Done summary

## Evidence
