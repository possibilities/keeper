/**
 * Impossible Git identity retaining a dropped root's consumed attribution
 * watermark while `git_status` is absent. Git paths cannot contain NUL.
 */
export const ATTRIBUTION_FLOOR_SESSION_ID = "\u0000keeper-attribution-floor";
export const ATTRIBUTION_FLOOR_PATH = "\u0000";
